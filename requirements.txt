Django>=5.0,<6.0
gunicorn>=23.0,<24.0
whitenoise>=6.7,<7.0
# Local .env loading. Existing process environment always wins (override=False).
python-dotenv>=1.0,<2.0
# Cognito administration and server-side password authentication.
boto3>=1.35,<2.0
# ID token verification (RS256 via JWKS). The crypto extra pulls `cryptography`.
PyJWT[crypto]>=2.9,<3.0
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0
lxml>=5.0,<6.0
bleach>=6.1,<7.0
# Optional Celery/Redis dispatcher for scrape runs (SCRAPER_USE_CELERY).
celery>=5.3,<6.0
redis>=5.0,<6.0
# AI-assisted scraper config from a URL (OPENAI_API_KEY).
openai>=1.0,<2.0
# Local .env loading. Existing environment variables still win (override=False).
python-dotenv>=1.0,<2.0
